# Anthropic API key for Claude AI integration
ANTHROPIC_API_KEY=

# Secret key for JWT session signing (use a long random string in production)
JWT_SECRET=

# Database URL (SQLite default — change for production databases)
DATABASE_URL="file:./dev.db"
